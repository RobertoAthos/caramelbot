services:
  caramelbot:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./skills:/app/skills
      - db-data:/app/data
    environment:
      - DATABASE_URL=sqlite:///data/caramelbot.db    
    restart: unless-stopped

volumes:
  db-data:
